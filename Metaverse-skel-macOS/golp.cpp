#include "metaverse.h"
#include <string>

#include <iostream>

 int count_neighbors(metaverse_t &board, int row, int column) {
  /* If you get linker errors, make sure that you have declared your functions
   * with the proper types.*/
  return 0;
};

 int count_neighbors(metaverse_t board, int row, int column) {
  /* If you get linker errors, make sure that you have declared your functions
   * with the proper types.*/

  return 0;
};

 metaverse_t tick(metaverse_t &board) {
  /* If you get linker errors, make sure that you have declared your functions
   * with the proper types.*/
  return metaverse_t{};
};

 metaverse_t tick(metaverse_t board) {
  /* If you get linker errors, make sure that you have declared your functions
   * with the proper types.*/
  return metaverse_t{};
};

bool configuration_row_to_metaverse_row(std::string &input_row, int row,
                                        metaverse_t &board) {
  return false;
}


// void read_metaverse_configuration_line_from_file() {
//   std::ifstream metaverse_file{"universe.meta"};
//     bool success_or_failure{false};
//     int generations{0};
//     int size{0};
//     success_or_failure = read_metaverse_configuration_line_from_file(metaverse_file, size, generations);
//     std::cout << "read_metaverse_configuration_line_from_file result : " << success_or_failure << "\n";
//     std::cout << "read_metaverse_configuration_line_from_file size : " << size << "\n";
//     std::cout << "read_metaverse_configuration_line_from_file generations: " << generations << "\n";
// }